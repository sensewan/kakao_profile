import 'package:flutter/material.dart';

class AnimationPageTest extends StatelessWidget {
  const AnimationPageTest({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
